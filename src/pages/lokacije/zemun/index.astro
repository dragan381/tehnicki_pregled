---
import LocationPage from '../../../components/LocationPage.astro';
import { getLocationBySlug } from '../../../utils/strapi';

const location = await getLocationBySlug('zemun');

if (!location) {
  return Astro.redirect('/404');
}
---

<LocationPage location={location} />
